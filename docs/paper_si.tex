\section*{Supporting Information}

\paragraph{Autocorrelation robustness: raw $n$ vs $n_{\mathrm{eff}}=n/g$ (Figure~\ref{fig:gsweep}).}
The sandwich $B=n_f\Var_f[p]+n_r\Var_r[p]$ (Theorem~\ref{thm:sand}) assumes independent samples,
whereas real molecular-dynamics works are autocorrelated. On the $19$ real BACE1 RBFE binding edges
of Figure~\ref{fig:A}B (no new MD), we recompute the sandwich se at the raw sample count and again
after decorrelating each leg to its statistical inefficiency $g$~\citep{shirts2008mbar}
(geometric-mean $\bar g=1.48$, range $[1.15,2.16]$; median count $n{=}500\to349$). Against the
correlation-aware truth (the decorrelated bootstrap SD, the same reference as Figure~\ref{fig:A}B),
the $n_{\mathrm{eff}}$ sandwich is calibrated (reported/truth $1.00$, $[0.93,1.07]$, reproducing
Figure~\ref{fig:A}B), while the raw-$n$ sandwich is overconfident by $0.83\times$ ($[0.67,0.97]$),
matching the $1/\sqrt{\bar g}=0.82$ autocorrelation prediction. The sandwich also tracks its
\emph{own} matched (raw) bootstrap ($1.00$, $[0.92,1.05]$): the closed form is internally
consistent, and the only quantity that must be right is the effective sample count, which the paper
decorrelates to $g$ throughout (Section~\ref{sec:bar}, paper text). The calibration verdict is
therefore robust to autocorrelation once $n_{\mathrm{eff}}$ is used, as it is in every real-data
panel. Deterministic; \texttt{make gsweep}.

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figAC_gsweep.pdf}
\caption{\textbf{Supplement: the sandwich calibration is robust to MD autocorrelation given
$n_{\mathrm{eff}}=n/g$ (real BACE1 edges).} (A) Sandwich se versus the correlation-aware
(decorrelated bootstrap) truth: at $n_{\mathrm{eff}}$ the sandwich lies on the $y{=}x$ line
($1.00\times$, calibrated), while at raw $n$ it sits below it ($0.83\times$, overconfident). (B) The
raw-$n$ overconfidence equals the $1/\sqrt{\bar g}$ autocorrelation factor ($\bar g=1.48$); because
the sandwich matches its own bootstrap under raw $n$ as well, the fix is the effective sample count
$n_{\mathrm{eff}}$, not the closed form.}
\label{fig:gsweep}
\end{figure}

\paragraph{Synthetic decomposition (illustration).} A controlled 1-D problem where the
generative structure is known shows the decomposed interval is up to $2.2\times$ sharper vs split-conformal ($2.0\times$ vs CQR) at equal coverage with better conditional calibration; this is an illustration of the
mechanism, not evidence on real data (Figure~\ref{fig:Breal} is the real test).

\paragraph{Edge weighting (Figure~\ref{fig:C}).} The acquisition policy beats random
($2.2\times$), but sandwich-vs-naive edge \emph{weighting} barely moves calls-to-top-$k$
($0.98\times$ $[0.93,1.03]$, 24-seed bootstrap CI): the Gauss--Markov ceiling.

\paragraph{Ligand-only target identification (Figure~\ref{fig:F}).} On real ChEMBL reverse
screening, a fingerprint model ties, but does not beat, a Tanimoto baseline (top-1
$0.84$--$0.97$ vs $0.86$--$0.96$), though $P(\text{binds})$ is well calibrated (ECE $0.008$).
Beating similarity needs orthogonal 3D/pocket information.

\begin{figure}[t]\centering
\includegraphics[width=0.7\textwidth]{figC_active_learning.pdf}
\caption{\textbf{Supplement: edge weighting is second-order for ranking.} The policy beats
random; the sandwich-vs-naive weighting does not move calls-to-top-$k$ (Gauss--Markov).}
\label{fig:C}
\end{figure}

\paragraph{Gauge-aware acquisition (Figure~\ref{fig:D}).} The Fisher--resistance Laplacian
(Theorem~\ref{thm:fr}) makes the knowledge gradient exactly zero on gauge (all-ones) contrasts; a
controlled simulation confirms $\mathrm{KG}=10^{-21}$ there and shows a gauge-\emph{unaware}
acquisition wastes budget resolving a decision-irrelevant cross-cluster offset (within-target
top-$k$ regret $0.27$ vs $0.01$).

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figD_gauge_identifiability.pdf}
\caption{\textbf{Supplement: gauge-aware acquisition (simulation).} (A) Knowledge gradient is
exactly $0$ on the gauge (all-ones) contrast and routes value to decision-relevant edges. (B)
Ablating gauge-awareness wastes budget on redundant cross-cluster bridges.}
\label{fig:D}
\end{figure}

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figF_target_id.pdf}
\caption{\textbf{Supplement: ligand-only reverse screening is similarity-bound.} (A) A
fingerprint model ties a Tanimoto baseline on hidden-target recovery. (B) $P(\text{binds})$ is
well calibrated (ECE $0.008$).}
\label{fig:F}
\end{figure}

\paragraph{Chirality completeness on the designed set (Figure~\ref{fig:E}).} An even
$O(3)$-invariant readout collapses every enantiomer pair exactly (collapse $=0$ to machine
precision, so its best possible chiral $\ddG$ prediction is zero); adding one parity-odd
$\texttt{0o}$ triple-product channel cuts enantiomer $\ddG$ MAE $11.7\times$ ($1.67\to0.14$
kcal/mol), realizing Theorem~\ref{thm:chiral}.

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figE_chirality_completeness.pdf}
\caption{\textbf{Supplement: a parity-odd channel is necessary and one triple product suffices.}
(A) The even ($O(3)$) readout collapses enantiomers exactly; $\texttt{0o}$ separates them.
(B) Enantiomer $\ddG$ MAE drops $11.7\times$ when the $\texttt{0o}$ channel is added.}
\label{fig:E}
\end{figure}

\paragraph{Chirality completeness on a real molecule (Figure~\ref{fig:Ereal}).}
Theorem~\ref{thm:chiral} is instantiated on thalidomide, the textbook case where the two
enantiomers differ in biological activity. We embed one enantiomer in 3D (RDKit) and reflect it
to the exact mirror geometry. Every O(3)-invariant readout is identical on the pair: all $171$
pairwise distances and all pairwise dot products (the Gram matrix) match to machine precision
($\max|\Delta|=0$), so a norm-and-dot-product readout is enantiomer-blind. The parity-odd
\texttt{0o} pseudoscalar $\chi=\det[p_1{-}p_0,p_2{-}p_0,p_3{-}p_0]$ flips sign
($+1.037\to-1.037$), and RDKit's CIP assignment flips $R\to S$. This reproduces, on real
chemistry, the collapse-and-restore of the chirality bit proved for the designed test set
(Figure~\ref{fig:E}). Deterministic; \texttt{make figEreal}.

\begin{figure}[t]\centering
\includegraphics[width=0.95\textwidth]{figE_chirality_real.pdf}
\caption{\textbf{Supplement: Theorem~\ref{thm:chiral} on a real chiral drug (thalidomide).}
An O(3)-invariant readout (all pairwise distances and dot products) is identical on the
enantiomer pair to machine precision; the parity-odd \texttt{0o} pseudoscalar $\chi$ flips sign
($+1.04\to-1.04$); RDKit CIP $R\to S$. The solid vs.\ hashed wedge at the stereocentre marks the
enantiomer.}
\label{fig:Ereal}
\end{figure}

\paragraph{Structural context of the flagged systems (Figure~\ref{fig:qcstruct}).}
The six systems the cycle-closure QC flags (Figure~\ref{fig:L}) are the ones carrying an
independently-documented hard structural feature, read here straight from public co-crystal
structures. In BACE1 (PDB 4DJW) the catalytic aspartic-acid dyad hydrogen-bonds the inhibitor
(Asp93 and Asp289, both $2.7$\,\AA); the protonation state of an aspartic-protease dyad is the
textbook source of free-energy error. In BRD4 BD1 (PDB 3MXF, in complex with
JQ1~\citep{filippakopoulos2010bet}) the acetyl-lysine pocket packs $16$ crystallographic waters
within $4.5$\,\AA\ of the ligand ($8$ within $3.5$\,\AA) around the recognition asparagine Asn140
($3.1$\,\AA); this conserved water network is the known BRD4 free-energy challenge~\citep{aldeghi2016binding}.
The other flags carry analogous a-priori difficulties: \texttt{faah} (covalent serine hydrolase,
large acyl channel), \texttt{cdk8} and \texttt{p38} (large scaffold and R-group changes), and
\texttt{hif2a} (a buried polar cavity). This is retrospective structural \emph{context} for why
the flags are chemically sensible, not a per-edge causal claim: cycle closure is edge-level and
blind to node-consistent bias (main text), and the point is only that the flagged set coincides
with the features a domain expert would flag a priori. Deterministic; \texttt{make qcstruct}
(public PDBs cached in \texttt{data/pdb/}).

\begin{figure}[t]\centering
\includegraphics[width=0.9\textwidth]{figQC_structures.pdf}
\caption{\textbf{Supplement: structural context of two flagged systems.} Heavy-atom coordinates
from public co-crystal structures. (A) BACE1 (PDB 4DJW): the catalytic aspartic dyad
(Asp93/Asp289, red) hydrogen-bonds the inhibitor (grey), a protonation-state difficulty. (B) BRD4
BD1 (PDB 3MXF, JQ1): the conserved acetyl-lysine-pocket waters (blue; $16$ within $4.5$\,\AA) and
the recognition Asn140 (green) pack against the ligand, the buried-water challenge.}
\label{fig:qcstruct}
\end{figure}

\paragraph{Per-edge observability map and predictive falsifier (Figure~\ref{fig:Lev}).} The
estimation--detection conservation law (Theorem~\ref{thm:cons}) certifies $\sum_e h_e=\mathrm{dof}$
to machine precision across the $48$ cyclic systems and lists the structurally un-auditable bridge
edges; a pre-registered test of whether the curl-leverage \emph{additionally} predicts the location
of reproducible systematic error is negative (studentized Spearman $\rho=-0.073$, $p=0.9571$: KILL),
so only the structural certificate is claimed.

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figLev_observability.pdf}
\caption{\textbf{Supplement: per-edge observability map and pre-registered predictive falsifier
(Theorem~\ref{thm:cons}).} (A) Structural result: across all $48$ systems with $\geq1$ independent
cycle, $\sum_e h_e=\mathrm{dof}$ holds to machine precision ($1.78\times10^{-14}$ max deviation), and
$48$ bridge edges (one structurally un-auditable, $h_e=0$, edge per system on average) are
identified. (B) Predictive falsifier: curl-leverage $h_e$ versus the studentized, cross-replicate
reproducible residual magnitude on $934$ pooled edges over $45$ systems (OpenFE
IndustryBenchmarks2024, the same $1145$-edge replicate set as Figure~\ref{fig:L}); studentized
Spearman $\rho=-0.073$, within-system block-permutation $p=0.9571$ ($10^4$ permutations) against the
pre-registered rule (CONFIRMED if $\rho>0$ and $p<0.05$): VERDICT KILL. Observability does not
predict where reproducible systematic error concentrates on this benchmark; the structural
certificate in panel A stands regardless.}
\label{fig:Lev}
\end{figure}

\paragraph{Close-the-loop accuracy race (Figure~\ref{fig:Lcausal}).} On the four flagged systems
groundable against public ChEMBL affinity, guided removal of the QC-flagged high-$|z|$ edges ties a
size-matched random removal on MUE-vs-experiment (combined Stouffer $p=0.48$): the calibrated QC
causally repairs internal cycle-closure (Figure~\ref{fig:Lval}) but not force-field-limited absolute
accuracy, the same scope boundary as the clean \texttt{eg5} system.

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figLcausal_guided_vs_random.pdf}
\caption{\textbf{Supplement: close-the-loop accuracy race, guided versus random QC-edge removal
(NULL).} On the four flagged systems groundable against public ChEMBL affinity (\texttt{cdk8, hif2a,
p38, bace}), removing the calibrated-QC-flagged high-$|z|$ edges (guided, fixed $K$ per system) does
not reduce MUE-vs-experiment more than a size-matched random removal (per-system one-sided
permutation $p$ shown; combined Stouffer $p=0.48$, sign test $p=0.69$). The calibrated QC causally
repairs internal network consistency (Figure~\ref{fig:Lval}) but does not, on this evidence, reduce
force-field-limited absolute accuracy against experiment, exactly because cycle closure is blind to
node-consistent force-field bias.}
\label{fig:Lcausal}
\end{figure}

\paragraph{Fixed-cutoff head-to-head and $\chi^2$ reconciliation (Figure~\ref{fig:Cut}).} Two
pre-registered peer-review checks on the same $1145$ edges. P1: the calibrated per-edge null (AUC
$0.865$ against a cross-replicate reproducibility anchor) does not measurably out-discriminate a
fixed $1.0$ kcal/mol hysteresis cutoff (AUC $0.575$) or a fixed-$\mathrm{se}$ $\chi^2$ test (AUC
$0.725$); paired bootstrap difference $+0.140$ $[-0.008,+0.316]$ (VERDICT: TIE). P2: the
shared-ligand-endpoint residual-correlation hypothesis for the closure-implied ($1.71\times$;
$1.67\times$ on the $46$-system subset where this diagnostic is estimable) versus
replicate-validated ($1.41\times$) se-conservatism gap is not confirmed (excess correlation
$-0.0079$ $[-0.0314,+0.0164]$; corrected factor $1.67\times$, VERDICT: NOT-CONFIRMED); the
Benjamini--Hochberg flag set is unchanged. The calibrated null flags $6$ of $48$ systems, the
fixed-$\mathrm{se}$ $\chi^2$ test $11$, and the fixed cutoff $26$; the three flag sets are
strictly nested (calibrated $\subset$ fixed-$\mathrm{se}$ $\subset$ fixed cutoff), so on this
benchmark the calibrated null catches no system the fixed cutoff misses and instead removes
$20$ of its flags at a higher AUC. The $1.0$ kcal/mol cutoff was pre-registered at the
field-standard value before this comparison was run, and no sweep over alternative thresholds
was performed, so the comparison above is specific to that one fixed rule.

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figCut_cutoff_benchmark.pdf}
\caption{\textbf{Supplement: fixed-cutoff head-to-head and $\chi^2$ reconciliation (peer-review
checks, same $1145$ edges).} (A) P1: Mann-Whitney AUC of each detector against the cross-replicate
reproducibility anchor. The calibrated per-edge null (AUC $0.865$) does not clear both foils, the
fixed $1.0$ kcal/mol hysteresis cutoff (AUC $0.575$) and the fixed-$\mathrm{se}$ $\chi^2$ test (AUC
$0.725$), at $95\%$ confidence (paired bootstrap difference $+0.140$ $[-0.008,+0.316]$): VERDICT
TIE. (B) P2: nominal versus shared-node-correlation-corrected (effective-dof) median reduced
$\chi^2$ and the implied se-conservatism factor, against the independently replicate-validated
factor. The correction leaves the factor at $1.67\times$ (nominal $1.67\times$ on the $46$-system
subset where the diagnostic is estimable), short of the replicate-validated $1.41\times$: VERDICT
NOT-CONFIRMED.}
\label{fig:Cut}
\end{figure}

\paragraph{QC sweep under the real learned-$\sigma$ profile (Figure~\ref{fig:P4}).}
Figure~\ref{fig:L}B's uniform ${\times}0.15$ shrink is a stress test; here the learned head's
\emph{measured}, overlap-dependent miscalibration profile (Figure~\ref{fig:A}A: ratio
$0.09$--$0.20\times$ across overlap $0.26$--$0.78$) is pushed through the identical GLS $+$
Benjamini--Hochberg pipeline, per edge, by percentile-rank transfer onto each of $1143$ (of the
$1145$; the sole cycle-free system contributes the other two) real
edges' own OpenFE \texttt{smallest\_overlap} (an approximation: the two overlap statistics are on
different scales, so only the profile's ordering and spread are transferred, not its raw values).
Four arms on the same $48$-system, $1143$-edge network: the calibrated sandwich (${\times}1$)
flags $6/48$ ($12\%$); the real profile (primary) flags $42/48$ ($88\%$), $7.00\times$ the
calibrated count, past the pre-registered $2\times$ threshold (VERDICT: DEGRADES); a
shuffled-profile control (the identical per-edge ratios with their association to overlap
destroyed, seed $20260808$) also flags $42/48$; and the uniform ${\times}0.15$ stress test again
flags $42/48$. The three shrinking arms are not identical sets of systems (real vs.\ shuffled and
real vs.\ uniform each swap exactly one system, \texttt{bace1} for \texttt{egfr}), so the per-edge
ratio assignment is genuinely heterogeneous; the near-identical flagged counts instead reflect
that the profile's entire measured range is deeply overconfident, inflating every edge's $\chi^2$
contribution by at least ${\sim}25\times$, so which systems cross the Benjamini--Hochberg
threshold is set by the magnitude of the shrink, not by its structure. Deterministic;
\texttt{make figP4}.

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figP4_sigma_profile.pdf}
\caption{\textbf{Supplement: the QC sweep under the learned head's real, non-uniform
miscalibration profile (peer-review item P4).} (Left) Four arms on the identical $48$-system,
$1143$-edge OpenFE network. Calibrated sandwich (${\times}1$): $6/48$ flagged ($12\%$). Real
learned profile (primary; percentile-rank transfer of Figure~\ref{fig:A}A's measured ratio onto
each edge's own overlap): $42/48$ ($88\%$), $7.00\times$ the calibrated count (VERDICT: DEGRADES
against the pre-registered $2\times$ threshold). Shuffled-profile control (association with
overlap destroyed): $42/48$. Uniform ${\times}0.15$ stress test (Figure~\ref{fig:L}B): $42/48$.
The rank transfer approximates how the head's measured overconfidence would vary across these
edges; it is not the head's own output on this benchmark. (Right) Histogram of the $1143$
per-edge transferred ratios (real learned profile), with Figure~\ref{fig:L}B's uniform
${\times}0.15$ stand-in marked (dashed line): the per-edge assignment is genuinely heterogeneous
(all $1143$ ratios are distinct), but the whole distribution sits in a narrow, uniformly
overconfident band ($0.09$--$0.20\times$). That narrow band is why the heterogeneous and uniform
stand-ins agree so closely in flagged count: it is the magnitude of the miscalibration, not the
uniformity of the Figure~\ref{fig:L}B stand-in, that destroys the test, and this figure shows the
band rather than only asserting it.}
\label{fig:P4}
\end{figure}

\paragraph{Dose-response of the QC to $\sigma$ miscalibration (Figure~\ref{fig:P4b}).}
Figure~\ref{fig:L}B and Supplementary Figure~\ref{fig:P4} each characterize the cycle-closure test
at a small number of scale points (${\times}1$ calibrated, ${\times}0.15$ stress test / real
profile); here a pre-registered, frozen grid of $16$ uniform $\mathrm{se}$ scale factors
($2.0$--$0.10\times$) is swept through the identical GLS $+$ Benjamini--Hochberg pipeline on the
same $48$-system OpenFE network. Two frozen readouts: $s_{\text{onset}}$, the largest scale that
flags strictly more systems than calibrated, and $s_{50}$, the largest scale at which at least half
the systems are flagged. $s_{\text{onset}}=0.9\times$ ($7/48$ vs.\ the calibrated $6/48$) is exactly
one system crossing the Benjamini--Hochberg threshold and is close to mechanically forced (a
uniform scale multiplies every system's $\chi^2$ by the same $1/\text{scale}^2$, so the first grid
point below $1.0$ at which any single borderline system's $p$-value crosses the cutoff is close to
guaranteed to register); it is not, on its own, evidence of broad sensitivity to mild
overconfidence. The substantive change is mid-curve: from $0.7\times$ to $0.3\times$ the flagged
fraction climbs from $12/48$ ($25\%$) to $30/48$ ($62\%$), reaching half the benchmark at
$s_{50}=0.3\times$. The learned head's measured, rank-transferred miscalibration band
($0.09$--$0.20\times$; Figure~\ref{fig:A}A, Supplementary Figure~\ref{fig:P4}) lies entirely below
$s_{50}$, so the head is not near the transition but well inside the saturated regime. Two sanity
anchors confirm the sweep reproduces the already-published test path: the ${\times}1.0$ row reads
$6/48$, matching Figure~\ref{fig:L}A's calibrated count, and the ${\times}0.15$ row reads $42/48$,
matching Figure~\ref{fig:L}B and Supplementary Figure~\ref{fig:P4}'s uniform/real-profile arms.
Deterministic; \texttt{make figP4b}.

\begin{figure}[t]\centering
\includegraphics[width=0.92\textwidth]{figP4b_dose_response.pdf}
\caption{\textbf{Supplement: dose-response of the cycle-closure QC to $\sigma$ miscalibration
(peer-review item P4b).} Flagged fraction of the $48$-system OpenFE network across a
pre-registered, frozen grid of $16$ uniform $\mathrm{se}$ scale factors ($2.0$--$0.10\times$;
$1.0=$ calibrated sandwich, lower $=$ more overconfident), swept through the identical GLS $+$
Benjamini--Hochberg pipeline as Figure~\ref{fig:L} and Supplementary Figure~\ref{fig:P4}. Vertical
dotted line: the calibrated scale ($1.0\times$, $6/48$ flagged). Vertical dashed line:
$s_{50}=0.3\times$, the largest scale at which at least half the systems ($30/48$) are flagged.
Shaded band: the learned head's measured, rank-transferred miscalibration range
($0.09$--$0.20\times$), which lies entirely below $s_{50}$. The other frozen readout,
$s_{\text{onset}}=0.9\times$ (the largest scale flagging more than the calibrated count), is
exactly one system crossing the Benjamini--Hochberg threshold ($7/48$ vs.\ $6/48$) and is close to
mechanically forced by the uniform rescaling of every system's $\chi^2$, so it should not be read
as evidence of broad sensitivity near calibration; the mid-curve climb ($12/48$ at $0.7\times$ to
$30/48$ at $0.3\times$) carries the real evidentiary weight. Two sanity anchors: the ${\times}1.0$
point matches Figure~\ref{fig:L}A's calibrated count ($6/48$) and the ${\times}0.15$ point matches
Figure~\ref{fig:L}B / Supplementary Figure~\ref{fig:P4}'s uniform/real-profile arms ($42/48$).
Deterministic; \texttt{make figP4b}.}
\label{fig:P4b}
\end{figure}

\paragraph{Reward re-ranking over a measured congeneric set (Figure~\ref{fig:K}).} Driving a
trajectory-balance GFlowNet over \texttt{eg5} (the most $\sigma$-varying evaluable target), the
calibrated LCB reward \emph{lowers} the true hit-rate (cal$-$raw $-0.110$) because $\sigma$ tracks
chemical novelty ($\mathrm{corr}(\sigma_{\text{total}},\text{affinity})=+0.42$), not low quality; an
LCB reward helps only when $\sigma$ flags low-quality candidates (Table~\ref{tab:audit}, main text).

\begin{figure}[t]\centering
\includegraphics[width=0.7\textwidth]{figK_calibrated_generation.pdf}
\caption{\textbf{Supplement: honest audit, reward re-ranking over a measured congeneric set (single
target \texttt{eg5}).} A calibrated reward lowers the GFlowNet's true hit-rate (cal $0.140$ vs raw
$0.250$; cal$-$raw $-0.110$, CI $[-0.151,-0.073]$) because $\sigma$ tracks chemical novelty
($\mathrm{corr}(\sigma_{\text{total}},\text{affinity})=+0.42$), not low quality.}
\label{fig:K}
\end{figure}
